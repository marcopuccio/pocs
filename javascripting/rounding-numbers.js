var roundUp = 1.5;

// Math.round method rounds the given float number to the nearest integer.
// if the number to be rounded has a half decimal part, this number will
// be rounded to the next integer value
var rounded = Math.round(roundUp);

console.log(rounded);